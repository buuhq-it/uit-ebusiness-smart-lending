e-business - team 6

```shell
npm install -g @angular/cli
ng new smart-lending-portal --no-standalone

```
```shell
echo "# uit-ebusiness-smart-lending" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/buuhq-it/uit-ebusiness-smart-lending.git
git push -u origin main
```
```shell
docker-compose up --build

```


```shell
cd src\be
.\mvnw clean install
.\mvnw -pl apps/process-engine-service spring-boot:run
.\mvnw clean package -pl apps/process-engine-service -am
.\mvnw clean install -pl apps/process-engine-service -am
```
```text
https://github.com/flowable/flowable-engine/releases

```